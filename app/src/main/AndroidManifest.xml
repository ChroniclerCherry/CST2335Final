<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cst2335final"
    android:usesCleartextTraffic="true">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".BingFavoritesDetails"></activity>
        <activity android:name=".BingPicture" />
        <activity android:name=".NewsReaderSearch" />
        <activity android:name=".NewsReaderFaves" />
        <activity android:name=".EmptyActivity" />
        <activity android:name=".NASADailyLoading" />
        <activity android:name=".NASADailyEmptyActivity" />
        <activity android:name=".NASADailyFavourites" />
        <activity android:name=".BingFavoritesList" />
        <activity android:name=".Bing_Virtual_Earth" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>